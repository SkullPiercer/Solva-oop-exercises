# Создайте класс ToDoList, у которого:
# есть атрибут tasks (список строк);
# есть метод add_task(task) — добавляет задачу;
# есть метод show_tasks() — возвращает задачи, разделённые переносом строки;
# есть метод remove_task(task) — удаляет задачу, если она есть в списке иначе ничего не делает.

class ToDoList:
    pass